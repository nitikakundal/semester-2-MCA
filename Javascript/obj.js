const student = {                               
    fullName : "Krish",                 //key : value || objName.key || obj[key]
    age : 23,
    profession : "student"
};

//const object keys can be updated but in case of const declaration can't be updated
let name = "nitika"  //let can't be re-declared but can be updated (Block scope)
var age = 23;  //can be re-declared and updated (Global scope)
const price = 150; //can't be re-declared and updated (Block -->{ } scope)          

console.log(name);